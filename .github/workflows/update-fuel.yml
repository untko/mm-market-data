name: Update fuel prices

on:
  schedule:
    # Daily at 08:17 Myanmar time, after Max Energy publishes the day's rates.
    - cron: "47 1 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: update-data
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Fetch fuel data
        run: python update.py --topics fuel

      - name: Commit updated data and dashboard
        run: |
          git config user.name "market-data-bot"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/ dashboard/market-trends.svg
          git diff --cached --quiet || git commit -m "chore(data): update fuel $(date -u +'%Y-%m-%d')"
          git push
